import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  category: {
    padding: 12,
    marginBottom: 8,
    borderWidth: 1,
    borderColor: "#007bff",
    borderRadius: 8,
    backgroundColor: "#fff",
  },
  text: {
    color: "#007bff",
    fontSize: 16,
    fontWeight: "500",
    textAlign: "center",
  },
});
